#One-way ANOVA

str(ANOVA1)
complete.cases(ANOVA1)
x<-ANOVA1[complete.cases(ANOVA1), ]
str(x)
x<-na.omit(ANOVA1)
complete.cases(x)

anova_one_way<-aov(x$`State Assessment Score`~x$Curriculum, data = x)
summary(anova_one_way)


tukey.test<-TukeyHSD(anova_one_way)
tukey.test


tapply(x$`State Assessment Score`, x$Curriculum, mean)
tapply(x$`State Assessment Score`, x$Curriculum, sd)
